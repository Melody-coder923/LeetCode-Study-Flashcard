```
class Solution:
    def countBits(self, n: int) -> List[int]:
        res = [0] * (n + 1)
        for i in range(1, n + 1):
            # i >> 1 右移一位（去掉最低位），i & 1 判断最低位是否为 1
            res[i] = res[i >> 1] + (i & 1)
        return res
```
